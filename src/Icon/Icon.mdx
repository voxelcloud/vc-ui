--- 
name: Icon
route: /vc-ui/
--- 

import { Playground, Props } from 'docz'
import Icon  from './'
import '../assets/fonts/iconfont.css'
import iconMap from './iconMap'

# Icon

## Props

<Props of={Icon} />

## More Props

[see details](https://material-ui.com/zh/api/icon/)

## iconMap config

```
const prefix = 'iconfont';
const createTheme = () => createMuiTheme({
  iconMap: {
    arrowBack: `${prefix} iconarrow_back-24px3`,
    arrowDown: `${prefix} iconic_arrow_drop_down_`,
    swap: `${prefix} iconswap-24px`,
  },
});

function Theme(props) {
  const { children } = props;
  const theme = createTheme();
  return (
    <ThemeProvider theme={theme}>
      {children}
    </ThemeProvider>
  );
}
```

## Basic usage 

<Playground>
  {
    () => (
      <div>
        <Icon name="search" color="primary" />
        <Icon name="search" color="secondary" />
        <Icon name="search" color="warning" />
        <Icon name="search" color="success" />
        <Icon name="search" color="info" />
        <Icon name="search" color="text" />
        <Icon className="iconfont iconic_search_px" color='disabled' />
        <br/>
       {
         Object.keys(iconMap).map((icon, i) => {
           return (
            <span key={icon} style={{ display: 'inline-block', width: '200px' }} >
               {icon}: <Icon name={icon} key={icon}/>
            </span>
           )
         })
       }
      </div>
    )
  }
</Playground>

