--- 
name: TextField
route: /vc-ui/
--- 

import { Playground, Props } from 'docz' 
import TextField  from './';
import MenuItem from '../MenuItem'
import { makeStyles } from '@material-ui/core/styles'


# TextField

## Props

<Props of={TextField} />

## More Props

[see details](https://material-ui.com/zh/api/text-field/)

## Basic usage 

<Playground>
  {() => {
    const [state, setState] = React.useState({ text: '', number: '', password: '', select: 10, readText: 'hello world' })
    const [readOnly, setReadOnly] = React.useState(true)
    const handleChange = (event, key) => {
      setState({ ...state, [key]: event.target.value });
    };
    return (
      <div>
        <TextField label='普通输入' type='text' value={state.text} error helperText='必填' onChange={e => handleChange(e, 'text')} />
        <br/>
        <TextField label='密码输入' required type='password' value={state.password} onChange={e => handleChange(e, 'password')} />
        <br/>
        <TextField label='数字输入' type='number' value={state.number} onChange={e => handleChange(e, 'number')} />
        <br/>
        <TextField label='下拉选择' type='select' value={state.select} onChange={e => handleChange(e, 'select')}>
          <MenuItem value={10}>Ten</MenuItem>
          <MenuItem value={20}>Twenty</MenuItem>
          <MenuItem value={30}>Thirty</MenuItem>
        </TextField>
        <br/>
        <TextField 
          label={readOnly ? '只读模式' : '可写模式'} 
          readOnly = {readOnly} 
          type='text' 
          value={state.readText} 
          helperText='注意: 只读模式下不可编辑' 
          onChange={e => handleChange(e, 'readText')}
        />
        <button onClick={() => setReadOnly(!readOnly)}>{readOnly ? '开启可写' : '开启只读' }</button>
        <br/>
      </div>
    )
  }}
</Playground>

